import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aistudio.gallery',
  appName: 'Gallery',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
